import { GdsLayoutTemplatePreview } from '@doneisbetter/gds';

export const Default = () => <GdsLayoutTemplatePreview />;

export const Titled = () => (
  <GdsLayoutTemplatePreview
    title="Page layout templates"
    description="Browse the governed page scaffolds available to every team."
  />
);
