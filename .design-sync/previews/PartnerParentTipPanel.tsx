import { PartnerParentTipPanel } from '@sovereignsquad/gds';

export const Default = () => (
  <PartnerParentTipPanel label="Parent tip">
    High chairs are available without asking, and there is a quiet corner with a
    changing table near the back. Arrive before 11am on weekends to avoid the
    stroller crowd.
  </PartnerParentTipPanel>
);

export const Accessibility = () => (
  <PartnerParentTipPanel label="Accessibility note">
    Step-free entrance from the side street, wide aisles for prams, and a single
    accessible restroom. Staff will help carry trays to your table on request.
  </PartnerParentTipPanel>
);
