import type { MantineThemeOverride } from "@mantine/core";
import { createPublicBrandTheme } from "@sovereignsquad/gds-theme/client";

const appThemeOverrides: MantineThemeOverride = {
  primaryColor: "brand",
  defaultRadius: "md",
  fontFamily: "var(--font-app), sans-serif",
  colors: {
    brand: ["#eef8f7", "#d7f0ed", "#b0e2dd", "#86d2cc", "#58c2bb", "#2baea6", "#1f8b85", "#166a66", "#0d4a47", "#062a29"]
  }
};

export const appTheme = createPublicBrandTheme({
  flatSurfaces: true,
  overrides: appThemeOverrides,
});
