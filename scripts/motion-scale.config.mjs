// Motion declarations that legitimately sit off the interaction scale.
//
// Keyed by `file:line`; if the line moves, the entry stops matching and the gate fails until re-examined.
export const MOTION_ALLOWLIST = {

};
